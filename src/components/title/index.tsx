import { titleBar } from "@/components/title/titleBar";
import { titleRoot } from "@/components/title/titleRoot";
import { titleText } from "@/components/title/titleText";

export const Title = {
  Root: titleRoot,
  Text: titleText,
  Bar: titleBar,
};
